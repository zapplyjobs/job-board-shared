# Auto Bump Submodule — fires after CI Gate passes on job-board-shared main.
# Dispatches bump-submodule.yml in jobs-aggregator-private with the new SHA.
#
# Prerequisite: GH_PAT secret must be added to job-board-shared repo settings
# (same token used in all other zapplyjobs repos — repo, workflow scopes).
#
# This eliminates manual submodule bump dispatches for routine job-board-shared pushes.
# Manual dispatch via `gh workflow run bump-submodule.yml` still works as fallback.
#
# Race safety: bump-submodule.yml has concurrency group (cancel-in-progress: false),
# so a manual dispatch during an auto-bump will queue, not conflict.

name: Auto Bump Submodule

on:
  workflow_run:
    workflows: ["CI Gate"]
    types: [completed]
    branches: [main]

jobs:
  auto-bump:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Get latest SHA
        id: sha
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          SHA=$(gh api repos/zapplyjobs/job-board-shared/commits?per_page=1 --jq '.[0].sha')
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "Latest job-board-shared SHA: $SHA"

      - name: Dispatch bump-submodule
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          gh workflow run bump-submodule.yml \
            --repo zapplyjobs/jobs-aggregator-private \
            --field target_sha="${{ steps.sha.outputs.sha }}"
          echo "Dispatched bump-submodule.yml with SHA ${{ steps.sha.outputs.sha }}"

      - name: Notify on failure
        if: failure()
        run: |
          echo "::error::Auto-bump dispatch failed. Manual bump may be needed."

# ZJP transcrypt — encrypt all source files
# NOTE: transcrypt registers the git filter as "crypt" (not "transcrypt")
*.js filter=crypt diff=crypt
*.json filter=crypt diff=crypt
*.jsonl filter=crypt diff=crypt
